# Repositórios (Adapters / Repositories)

Esta camada contém os adaptadores de saída (Driven Adapters) para persistência.

## Conteúdo
- Implementações concretas das interfaces de repositório definidas em `core/ports/repositories`.
- Exemplo: `PrismaUserRepository`, `InMemoryContractRepository`.
